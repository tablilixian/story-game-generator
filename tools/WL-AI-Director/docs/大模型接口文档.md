# WL-AI-Director 大模型接口文档

## 📋 文档概述

本文档详细记录了 WL-AI-Director 项目中所有与大模型交互的接口，包括对话模型、图片生成模型和视频生成模型的完整调用信息。

**文档版本**: 1.0  
**最后更新**: 2026-04-09  
**项目路径**: `/Users/wl/Desktop/job/learn/WL-AI-Director`

---

## 🏢 支持的模型提供商

### 1. AntSK（默认提供商）

- **提供商ID**: `antsk`
- **基础URL**: `https://api.antsk.cn`
- **特点**: 默认提供商，支持 GPT、Claude、Gemini 等主流模型
- **认证方式**: Bearer Token

### 2. BigModel（智谱AI）

- **提供商ID**: `bigmodel`
- **基础URL**: `https://open.bigmodel.cn`
- **开发代理URL**: `/bigmodel`（避免 CORS）
- **特点**: 提供免费模型，支持 GLM、CogView、CogVideo 等
- **认证方式**: Bearer Token

---

## 💬 对话模型接口

### 接口概述

对话模型用于剧本解析、分镜生成、关键帧优化、动作生成等文本生成任务。

### 核心文件

- **API核心**: `services/ai/apiCore.ts`
- **适配器**: `services/adapters/chatAdapter.ts`
- **业务服务**: 
  - `services/ai/scriptService.ts` - 剧本处理
  - `services/ai/shotService.ts` - 分镜辅助
  - `services/ai/visualService.ts` - 视觉资产生成

### 1. Chat Completion API（非流式）

#### 接口信息

| 项目 | 内容 |
|------|------|
| **端点** | `/v1/chat/completions` |
| **方法** | POST |
| **超时** | 600000ms (10分钟) |
| **认证** | Bearer Token |

#### 请求参数

```typescript
{
  model: string,              // 模型名称，如 "gpt-5.1", "glm-4-flash"
  messages: Array<{
    role: 'user' | 'system' | 'assistant',
    content: string | Array<{
      type: 'text' | 'image_url',
      text?: string,
      image_url?: { url: string }
    }>
  }>,
  temperature: number,        // 0-2，默认 0.7
  max_tokens?: number,        // 最大token数，可选
  response_format?: {         // 响应格式，可选
    type: 'json_object'
  }
}
```

#### 响应格式

```typescript
{
  choices: Array<{
    message: {
      role: 'assistant',
      content: string
    },
    finish_reason: string
  }>,
  usage?: {
    prompt_tokens: number,
    completion_tokens: number,
    total_tokens: number
  }
}
```

#### 调用示例

```typescript
// 基础调用
const response = await chatCompletion(
  "请分析这段剧本...",
  "gpt-5.1",           // 模型ID
  0.7,                 // temperature
  8192,                // maxTokens
  'json_object'        // responseFormat
);

// 带图片的调用（用于视频生成）
const messages = [
  {
    role: 'user',
    content: [
      { type: 'text', text: '生成视频...' },
      { type: 'image_url', image_url: { url: 'data:image/png;base64,...' } }
    ]
  }
];
```

### 2. Chat Completion Stream API（流式）

#### 接口信息

| 项目 | 内容 |
|------|------|
| **端点** | `/v1/chat/completions` |
| **方法** | POST |
| **超时** | 600000ms (10分钟) |
| **认证** | Bearer Token |
| **特点** | SSE (Server-Sent Events) 流式响应 |

#### 请求参数

```typescript
{
  model: string,
  messages: Array<{ role: string, content: string }>,
  temperature: number,
  stream: true,              // 启用流式
  response_format?: {
    type: 'json_object'
  }
}
```

#### 响应格式（SSE）

```
data: {"choices":[{"delta":{"content":"文本内容"}}]}

data: {"choices":[{"delta":{"content":"更多内容"}}]}

data: [DONE]
```

#### 调用示例

```typescript
const fullText = await chatCompletionStream(
  "生成长文本...",
  "gpt-5.1",
  0.7,
  undefined,
  600000,
  (delta) => {
    console.log('收到增量:', delta);
  }
);
```

### 3. 支持的对话模型列表

#### AntSK 提供商

| 模型ID | 显示名称 | 特点 | 默认参数 |
|--------|----------|------|----------|
| `gpt-5.1` | GPT-5.1 | 剧本脚本切分首选，结构化输出稳定 | temperature: 0.7 |
| `gpt-5.2` | GPT-5.2 | 创意增强型，适合多种切分方案 | temperature: 0.7 |
| `gpt-41` | GPT-4.1 | 严谨切分，适合复杂叙事 | temperature: 0.7 |
| `claude-sonnet-4-5-20250929` | Claude Sonnet 4.5 | 长文友好，文字表达细腻 | temperature: 0.7 |

#### BigModel 提供商

| 模型ID | 显示名称 | 特点 | 端点 |
|--------|----------|------|------|
| `glm-4-plus` | GLM-4 Plus | 高性能对话模型 | `/api/paas/v4/chat/completions` |
| `glm-4-air` | GLM-4 Air | 高性价比，价格仅为 50% | `/api/paas/v4/chat/completions` |
| `glm-4-flash` | GLM-4 Flash | **免费**，快速响应 | `/api/paas/v4/chat/completions` |
| `glm-4` | GLM-4 | 稳定可靠 | `/api/paas/v4/chat/completions` |

---

## 🖼️ 图片生成接口

### 接口概述

图片生成模型用于生成角色、场景、关键帧等视觉资产。

### 核心文件

- **适配器**: `services/adapters/imageAdapter.ts`
- **业务服务**: `services/ai/visualService.ts`

### 1. Gemini Image API

#### 接口信息

| 项目 | 内容 |
|------|------|
| **提供商** | AntSK |
| **端点** | `/v1beta/models/{model}:generateContent` |
| **方法** | POST |
| **超时** | 600000ms (10分钟) |
| **认证** | Bearer Token |

#### 请求参数

```typescript
{
  contents: [{
    role: 'user',
    parts: Array<{
      text?: string,           // 提示词
      inlineData?: {           // 图片数据（参考图）
        mimeType: 'image/png' | 'image/jpeg',
        data: string           // base64编码
      }
    }>
  }],
  generationConfig: {
    responseModalities: ['TEXT', 'IMAGE'],
    imageConfig?: {
      aspectRatio: '16:9' | '9:16' | '1:1'
    }
  }
}
```

#### 响应格式

```typescript
{
  candidates: [{
    content: {
      parts: [{
        inlineData?: {
          mimeType: 'image/png',
          data: string           // base64编码的图片
        }
      }]
    }
  }]
}
```

#### 调用示例

```typescript
const imageUrl = await callImageApi({
  prompt: "生成一个动漫风格的角色...",
  referenceImages: ['data:image/png;base64,...'],  // 参考图
  aspectRatio: '16:9',
  resourceType: 'character',
  resourceId: 'char-001'
});
```

### 2. BigModel CogView API

#### 接口信息

| 项目 | 内容 |
|------|------|
| **提供商** | BigModel |
| **端点** | `/api/paas/v4/images/generations` |
| **方法** | POST |
| **开发代理** | `/bigmodel` |
| **认证** | Bearer Token |

#### 请求参数

```typescript
{
  model: string,              // 'cogview-3-flash', 'cogview-4' 等
  prompt: string,             // 提示词
  size: string                // '1280x720', '720x1280', '1024x1024'
}
```

#### 响应格式

```typescript
{
  data: [{
    url: string               // 图片URL
  }]
}
```

#### 调用示例

```typescript
const imageUrl = await callImageApi({
  prompt: "生成一个科幻风格的场景...",
  referenceImages: [],       // 可选，角色/场景参考图，用于一致性控制
  aspectRatio: '16:9',        // 可选，默认 '16:9'
  resourceType: 'keyframe',   // 可选，资源类型
  resourceId: 'kf-001'        // 可选，资源ID
});

// 返回格式: local:{imageId}
// 图片实际保存到本地 IndexedDB，并非直接返回 URL
```

#### 同步/异步说明

| 模式 | 说明 |
|------|------|
| **同步** | CogView API 是同步的，直接返回图片 URL，无需轮询 |

### 3. 支持的图片模型列表

#### AntSK 提供商

| 模型ID | 显示名称 | 端点 | 支持的宽高比 |
|--------|----------|------|--------------|
| `gemini-3-pro-image-preview` | Gemini 3 Pro Image | `/v1beta/models/gemini-3-pro-image-preview:generateContent` | 16:9, 9:16 |

#### BigModel 提供商

| 模型ID | 显示名称 | 特点 | 端点 |
|--------|----------|------|------|
| `cogview-3-flash` | CogView-3 Flash | **免费**，快速生成 | `/api/paas/v4/images/generations` |
| `cogview-4` | CogView-4 | 支持多种风格 | `/api/paas/v4/images/generations` |
| `cogview-3-plus` | CogView-3 Plus | 高质量生成 | `/api/paas/v4/images/generations` |
| `cogview-3` | CogView-3 | 标准模型 | `/api/paas/v4/images/generations` |

### 4. 图片尺寸映射

| 宽高比 | 尺寸（像素） |
|--------|--------------|
| 16:9 | 1280x720 |
| 9:16 | 720x1280 |
| 1:1 | 1024x1024 |

---

## 🎬 视频生成接口

### 接口概述

视频生成模型支持同步（Veo）和异步（Sora、BigModel）两种模式，用于生成分镜视频片段。

### 核心文件

- **API核心**: `services/ai/videoService.ts`
- **适配器**: `services/adapters/videoAdapter.ts`
- **Edge Function**: `supabase/functions/generate-video/index.ts`

### 1. Veo 同步模式

#### 接口信息

| 项目 | 内容 |
|------|------|
| **提供商** | AntSK |
| **端点** | `/v1/chat/completions` |
| **方法** | POST |
| **超时** | 1200000ms (20分钟) |
| **认证** | Bearer Token |
| **特点** | 同步返回，支持首尾帧 |

#### 请求参数

```typescript
{
  model: string,              // 'veo_3_1_t2v_fast_landscape' 等
  messages: [{
    role: 'user',
    content: string | Array<{
      type: 'text' | 'image_url',
      text?: string,
      image_url?: { url: string }
    }>
  }],
  stream: false,
  temperature: 0.7
}
```

#### 响应格式

```typescript
{
  choices: [{
    message: {
      content: string         // 包含视频URL的文本
    }
  }]
}
```

#### Veo 模型命名规则

| 场景 | 模型名称格式 | 示例 |
|------|--------------|------|
| 文本生成视频（横屏） | `veo_3_1_t2v_fast_landscape` | 无参考图，16:9 |
| 文本生成视频（竖屏） | `veo_3_1_t2v_fast_portrait` | 无参考图，9:16 |
| 图片生成视频（横屏） | `veo_3_1_i2v_s_fast_fl_landscape` | 有参考图，16:9 |
| 图片生成视频（竖屏） | `veo_3_1_i2v_s_fast_fl_portrait` | 有参考图，9:16 |

### 2. Sora 异步模式

#### 接口信息

| 项目 | 内容 |
|------|------|
| **提供商** | AntSK |
| **端点** | `/v1/videos` |
| **方法** | POST |
| **超时** | 1200000ms (20分钟) |
| **认证** | Bearer Token |
| **特点** | 异步轮询，支持多种时长 |

#### 创建任务请求参数

```typescript
// FormData 格式
{
  model: string,              // 'sora-2', 'veo_3_1-fast'
  prompt: string,             // 提示词
  seconds: string,            // 时长：'4', '8', '12'
  size: string,               // 尺寸：'1280x720'
  input_reference?: Blob,     // 参考图（单图）
  input_reference[]?: Blob    // 参考图数组（首尾帧）
}
```

#### 创建任务响应

```typescript
{
  id: string,                 // 任务ID
  task_id?: string
}
```

#### 查询任务状态

**端点**: `GET /v1/videos/{taskId}`

**响应**:

```typescript
{
  status: 'processing' | 'completed' | 'failed',
  progress?: number,          // 0-100
  video_url?: string,         // 完成后的视频URL
  video_id?: string,
  error?: {
    message: string
  }
}
```

#### 下载视频

**端点**: `GET /v1/videos/{videoId}/content`

**响应**: 视频文件流

### 3. BigModel 视频生成

#### 接口信息

| 项目 | 内容 |
|------|------|
| **提供商** | BigModel |
| **端点** | `/api/paas/v4/videos/generations` |
| **开发代理** | `/bigmodel` |
| **方法** | POST |
| **认证** | Bearer Token |

#### 创建任务请求参数

```typescript
{
  model: string,              // 'cogvideox', 'vidu' 等
  prompt: string,
  duration: number,           // 时长
  size: string,               // 尺寸
  movement_amplitude: 'auto',
  image_url?: string | string[]  // 参考图（base64格式）
}
```

#### 查询任务状态

**端点**: `GET /api/paas/v4/async-result/{taskId}`

**响应**:

```typescript
{
  task_status: 'PROCESSING' | 'SUCCESS' | 'FAIL',
  video_result?: [{
    url: string
  }],
  error?: {
    message: string
  }
}
```

### 4. 支持的视频模型列表

| 模型ID | 显示名称 | 模式 | 提供商 | 支持的宽高比 | 支持的时长 |
|--------|----------|------|--------|--------------|------------|
| `veo` | Veo 3.1 首尾帧 | 同步 | AntSK | 16:9, 9:16 | 8秒 |
| `veo_3_1-fast` | Veo 3.1 Fast | 异步 | AntSK | 16:9, 9:16 | 8秒 |
| `sora-2` | Sora 2 | 异步 | AntSK | 16:9, 9:16, 1:1 | 4, 8, 12秒 |
| `cogvideox` | CogVideoX | 异步 | BigModel | 16:9, 9:16 | 4, 5, 8, 10秒 |
| `vidu` | Vidu | 异步 | BigModel | 16:9, 9:16 | 4, 5, 8, 10秒 |

### 5. 视频尺寸映射

| 宽高比 | 尺寸（像素） |
|--------|--------------|
| 16:9 | 1280x720 |
| 9:16 | 720x1280 |
| 1:1 | 720x720 |

---

## 🎯 业务功能接口

### 1. 剧本解析接口

**服务文件**: `services/ai/scriptService.ts`

#### parseScriptToData

**功能**: 解析原始文本为结构化剧本数据

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  rawText: string,            // 原始剧本文本
  language: string,           // 语言：'中文', 'English'
  model?: string,             // 模型ID
  visualStyle: string         // 视觉风格：'live-action', 'anime' 等
}
```

**下行响应**:
```typescript
{
  title: string,
  genre: string,
  logline: string,
  language: string,
  artDirection: ArtDirection,  // 美术指导文档
  characters: Character[],     // 角色列表（含视觉提示词）
  scenes: Scene[],             // 场景列表（含视觉提示词）
  props: Prop[],
  storyParagraphs: StoryParagraph[]
}
```

**API调用流程**:
1. 调用 Chat API 解析剧本结构
2. 生成全局美术指导文档（Art Direction）
3. 批量生成角色视觉提示词
4. 生成场景视觉提示词

### 2. 分镜生成接口

**服务文件**: `services/ai/scriptService.ts`

#### generateShotList

**功能**: 根据剧本数据生成分镜列表

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  scriptData: ScriptData,     // 剧本数据
  model?: string              // 模型ID
}
```

**下行响应**:
```typescript
Array<{
  id: string,
  sceneId: string,
  actionSummary: string,
  dialogue: string,
  cameraMovement: string,
  shotSize: string,
  characters: string[],
  keyframes: Array<{
    id: string,
    type: 'start' | 'end',
    visualPrompt: string,
    status: 'pending' | 'generating' | 'completed' | 'failed'
  }>
}>
```

### 3. 关键帧优化接口

**服务文件**: `services/ai/shotService.ts`

#### optimizeBothKeyframes

**功能**: AI一次性优化起始帧和结束帧视觉描述

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  actionSummary: string,      // 动作描述
  cameraMovement: string,     // 镜头运动
  sceneInfo: {
    location: string,
    time: string,
    atmosphere: string
  },
  characterInfo: string[],    // 角色信息
  visualStyle: string,        // 视觉风格
  model?: string
}
```

**下行响应**:
```typescript
{
  startPrompt: string,        // 起始帧描述
  endPrompt: string           // 结束帧描述
}
```

### 4. 动作生成接口

**服务文件**: `services/ai/shotService.ts`

#### generateActionSuggestion

**功能**: AI生成叙事动作建议

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  startFramePrompt: string,   // 起始帧描述
  endFramePrompt: string,     // 结束帧描述
  cameraMovement: string,     // 镜头运动
  model?: string
}
```

**下行响应**:
```typescript
string  // 动作描述文本
```

### 5. 镜头拆分接口

**服务文件**: `services/ai/shotService.ts`

#### splitShotIntoSubShots

**功能**: 将单个镜头拆分为多个细致的子镜头

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  shot: any,                  // 原始镜头数据
  sceneInfo: {
    location: string,
    time: string,
    atmosphere: string
  },
  characterNames: string[],
  visualStyle: string,
  model?: string
}
```

**下行响应**:
```typescript
{
  subShots: Array<{
    shotSize: string,
    cameraMovement: string,
    actionSummary: string,
    visualFocus: string,
    keyframes: Array<{
      type: 'start' | 'end',
      visualPrompt: string
    }>
  }>
}
```

### 6. 美术指导文档生成接口

**服务文件**: `services/ai/visualService.ts`

#### generateArtDirection

**功能**: 生成全局美术指导文档

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  title: string,
  genre: string,
  logline: string,
  characters: Array<{
    name: string,
    gender: string,
    age: string,
    personality: string
  }>,
  scenes: Array<{
    location: string,
    time: string,
    atmosphere: string
  }>,
  visualStyle: string,
  language: string,
  model?: string
}
```

**下行响应**:
```typescript
{
  colorPalette: {
    primary: string,
    secondary: string,
    accent: string,
    skinTones: string,
    saturation: string,
    temperature: string
  },
  characterDesignRules: {
    proportions: string,
    eyeStyle: string,
    lineWeight: string,
    detailLevel: string
  },
  lightingStyle: string,
  textureStyle: string,
  moodKeywords: string[],
  consistencyAnchors: string
}
```

### 7. 角色九宫格生成接口

**服务文件**: `services/ai/visualService.ts`

#### generateCharacterTurnaroundPanels

**功能**: 生成角色九宫格造型设计面板描述

**调用模型**: 对话模型（Chat）

**上行参数**:
```typescript
{
  character: Character,
  artDirection: ArtDirection,
  visualStyle: string,
  language: string,
  model?: string
}
```

**下行响应**:
```typescript
Array<{
  index: number,              // 0-8
  viewAngle: string,          // '正面', '左侧面', '背面' 等
  shotSize: string,           // '全身', '半身特写', '面部特写'
  description: string
}>
```

### 8. 图片生成接口

**服务文件**: `services/ai/visualService.ts`

#### generateImage

**功能**: 生成图片（角色、场景、关键帧等）

**调用模型**: 图片模型（Image）

**上行参数**:
```typescript
{
  prompt: string,
  referenceImages: string[],  // 参考图（base64或URL）
  aspectRatio: '16:9' | '9:16' | '1:1',
  isVariation: boolean,       // 是否为变体
  hasTurnaround: boolean,     // 是否有九宫格
  resourceType: string,       // 'character', 'scene', 'prop', 'keyframe'
  resourceId: string
}
```

**下行响应**:
```typescript
string  // 图片URL（格式：local:{id} 或 https://...）
```

### 9. 视频生成接口

**服务文件**: `services/ai/videoService.ts`

#### generateVideo

**功能**: 生成视频片段

**调用模型**: 视频模型（Video）

**上行参数**:
```typescript
{
  prompt: string,
  startImageBase64?: string,  // 起始帧图片（base64）
  endImageBase64?: string,    // 结束帧图片（base64）
  model: string,              // 模型ID
  aspectRatio: '16:9' | '9:16' | '1:1',
  duration: 4 | 5 | 8 | 10 | 12
}
```

**下行响应**:
```typescript
string  // 视频URL或base64数据
```

---

## 🔧 API Key 管理

### 1. API Key 存储位置

- **全局 API Key**: `localStorage` 键名 `antsk_api_key`
- **模型专属 API Key**: 存储在模型注册中心（`bigbanana_model_registry`）
- **提供商 API Key**: 存储在提供商配置中

### 2. API Key 获取优先级

1. 模型专属 API Key（`model.apiKey`）
2. 提供商 API Key（`provider.apiKey`）
3. 全局 API Key（`globalApiKey`）

### 3. API Key 验证接口

**服务文件**: `services/ai/apiCore.ts`

#### verifyApiKey

**功能**: 验证 API Key 的连通性

**上行参数**:
```typescript
{
  key: string  // API Key
}
```

**下行响应**:
```typescript
{
  success: boolean,
  message: string
}
```

**验证流程**:
1. 发送测试请求到 Chat API
2. 检查返回结果
3. 返回验证状态

---

## ⚠️ 错误处理

### 1. 错误类型

#### ApiKeyError

```typescript
class ApiKeyError extends Error {
  name: 'ApiKeyError';
  message: string;
}
```

**触发场景**: API Key 缺失或无效

#### HTTP 错误

| 状态码 | 含义 | 处理方式 |
|--------|------|----------|
| 400 | 客户端错误 | 不重试，直接返回错误信息 |
| 401 | 认证失败 | 不重试，提示 API Key 错误 |
| 403 | 权限不足 | 不重试，提示权限问题 |
| 429 | 限流 | 自动重试（指数退避） |
| 500 | 服务器错误 | 自动重试（指数退避） |
| 504 | 网关超时 | 自动重试（指数退避） |

### 2. 错误响应格式

```typescript
{
  error: {
    message: string,
    code?: string,
    type?: string
  }
}
```

### 3. 常见错误消息

| 错误消息 | 原因 | 解决方案 |
|----------|------|----------|
| "API Key 缺失，请在模型配置中设置 API Key" | 未配置 API Key | 在设置页面配置 API Key |
| "提示词可能包含不安全或违规内容" | 内容审核未通过 | 修改提示词，避免敏感词汇 |
| "当前请求较多，暂时未能处理成功" | 服务器繁忙 | 稍后重试 |
| "请求超时" | 网络或服务器响应慢 | 检查网络，稍后重试 |
| "视频生成超时 (20分钟)" | 视频生成时间过长 | 稍后重试或联系支持 |

---

## 🔄 重试机制

### 1. 重试策略

**服务文件**: `services/ai/apiCore.ts`

#### retryOperation

**参数**:
```typescript
{
  operation: () => Promise<T>,  // 要执行的操作
  maxRetries: number,           // 最大重试次数，默认 3
  baseDelay: number             // 基础延迟（毫秒），默认 2000
}
```

**指数退避算法**:
- 第 1 次重试: 2000ms
- 第 2 次重试: 4000ms
- 第 3 次重试: 8000ms

### 2. 可重试的错误

- HTTP 429（限流）
- HTTP 500（服务器错误）
- HTTP 504（网关超时）
- 网络错误（ECONNRESET, ETIMEDOUT）
- 包含特定关键词的错误：
  - 'quota'
  - 'RESOURCE_EXHAUSTED'
  - '超时'
  - 'timeout'
  - 'Gateway Timeout'

### 3. 不可重试的错误

- HTTP 400（客户端错误）
- HTTP 401（认证失败）
- HTTP 403（权限不足）
- 包含敏感内容错误

---

## 🌐 开发环境代理配置

### 1. Vite 代理配置

**配置文件**: `vite.config.ts`

```typescript
{
  proxy: {
    '/bigmodel': {
      target: 'https://open.bigmodel.cn',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/bigmodel/, '')
    },
    '/bigmodel-files': {
      target: 'https://aigc-files.bigmodel.cn',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/bigmodel-files/, '')
    },
    '/video-proxy': {
      target: 'https://maas-watermark-prod-new.cn-wlcb.ufileos.com',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/video-proxy/, '')
    }
  }
}
```

### 2. 代理用途

| 代理路径 | 目标 | 用途 |
|----------|------|------|
| `/bigmodel` | `https://open.bigmodel.cn` | BigModel API 调用 |
| `/bigmodel-files` | `https://aigc-files.bigmodel.cn` | BigModel 文件下载 |
| `/video-proxy` | `https://maas-watermark-prod-new.cn-wlcb.ufileos.com` | UCloud 视频下载 |

---

## 📊 日志记录

### 1. 日志分类

**服务文件**: `services/logger.ts`

```typescript
enum LogCategory {
  AI = 'AI',           // AI 相关
  VIDEO = 'VIDEO',     // 视频相关
  MODEL = 'MODEL',     // 模型相关
  CANVAS = 'CANVAS',   // 画布相关
  STORAGE = 'STORAGE', // 存储相关
  RENDER = 'RENDER'    // 渲染相关
}
```

### 2. 日志级别

- `debug`: 调试信息
- `info`: 一般信息
- `warn`: 警告信息
- `error`: 错误信息

### 3. 渲染日志

**服务文件**: `services/renderLogService.ts`

```typescript
{
  type: string,              // 日志类型
  resourceId: string,        // 资源ID
  resourceName: string,      // 资源名称
  status: 'success' | 'failed' | 'processing',
  model?: string,            // 使用的模型
  prompt?: string,           // 提示词
  error?: string,            // 错误信息
  duration: number,          // 耗时（毫秒）
  tokens?: {                 // Token 使用情况
    prompt: number,
    completion: number,
    total: number
  }
}
```

---

## 🚀 性能优化建议

### 1. 并发控制

- 批量生成角色提示词时，使用串行处理避免 API 限流
- 场景提示词生成间隔 1500ms

### 2. 缓存策略

- 模型注册中心状态缓存在内存中
- 图片存储在本地 IndexedDB（格式：`local:{id}`）

### 3. 超时设置

| 操作类型 | 超时时间 |
|----------|----------|
| Chat Completion | 600000ms (10分钟) |
| 图片生成 | 600000ms (10分钟) |
| 视频生成 | 1200000ms (20分钟) |
| 视频下载 | 600000ms (10分钟) |

### 4. 资源清理

- 及时清理 AbortController
- 清理超时定时器
- 关闭响应流

---

## 📝 附录

### A. 完整的模型参数配置

#### ChatModelParams

```typescript
interface ChatModelParams {
  temperature: number;           // 0-2，默认 0.7
  maxTokens?: number;            // 最大 token
  topP?: number;                 // Top P 采样
  frequencyPenalty?: number;     // 频率惩罚 0-2
  presencePenalty?: number;      // 存在惩罚 0-2
}
```

#### ImageModelParams

```typescript
interface ImageModelParams {
  defaultAspectRatio: AspectRatio;
  supportedAspectRatios: AspectRatio[];
}
```

#### VideoModelParams

```typescript
interface VideoModelParams {
  mode: 'sync' | 'async';
  defaultAspectRatio: AspectRatio;
  supportedAspectRatios: AspectRatio[];
  defaultDuration: VideoDuration;
  supportedDurations: VideoDuration[];
}
```

### B. 类型定义

```typescript
type AspectRatio = '16:9' | '9:16' | '1:1';
type VideoDuration = 4 | 5 | 8 | 10 | 12;
type VideoMode = 'sync' | 'async';
type ModelType = 'chat' | 'image' | 'video';
```

### C. 相关文件索引

| 文件路径 | 功能 |
|----------|------|
| `services/ai/apiCore.ts` | API 核心功能（重试、错误处理、JSON清理） |
| `services/ai/scriptService.ts` | 剧本解析、分镜生成 |
| `services/ai/shotService.ts` | 关键帧优化、动作生成、镜头拆分 |
| `services/ai/visualService.ts` | 视觉资产生成（角色、场景、九宫格） |
| `services/ai/videoService.ts` | 视频生成 |
| `services/adapters/chatAdapter.ts` | 对话模型适配器 |
| `services/adapters/imageAdapter.ts` | 图片模型适配器 |
| `services/adapters/videoAdapter.ts` | 视频模型适配器 |
| `services/modelRegistry.ts` | 模型注册中心 |
| `types/model.ts` | 模型类型定义 |
| `services/logger.ts` | 日志服务 |
| `supabase/functions/generate-video/index.ts` | 视频生成 Edge Function |

---

## 📞 技术支持

如有问题，请查看：
- 项目文档：`docs/` 目录
- 类型定义：`types/model.ts`
- 日志输出：浏览器控制台

---

**文档结束**
