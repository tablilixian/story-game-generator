# 更新日志

## [2026-03-09] - 修复本地图片加载错误和数据库配置优化

### 新增功能
- ✅ 统一数据库配置管理
  - 创建 dbConfig.ts 集中管理数据库配置
  - 统一 DB_VERSION 配置，避免版本冲突
  - 统一 STORE_NAMES 配置，便于维护

### 修复问题
- ✅ 修复本地图片加载错误 (ERR_UNKNOWN_URL_SCHEME)
  - 创建 KeyframeImage 组件，使用 useImageLoader 正确加载关键帧图片
  - 创建 AssetLibraryImage 组件，修复资产库图片加载
  - 创建 VariationImage 组件，修复角色变体图片加载
  - 统一使用 useImageLoader hook 处理 local:img_xxx 格式的图片引用
  - 移除直接使用 referenceImage 的代码，避免浏览器尝试加载无效协议

### 改进优化
- ✅ 优化图片加载流程
  - 所有本地图片都通过 useImageLoader hook 处理
  - 自动检测 local:img_xxx 格式并从 IndexedDB 加载
  - 自动转换为浏览器可加载的 blob: URL

### 技术细节
- 创建 `services/dbConfig.ts` 统一数据库配置
- 创建 `KeyframeImage` 组件用于关键帧图片显示
- 创建 `AssetLibraryImage` 组件用于资产库图片显示
- 创建 `VariationImage` 组件用于角色变体图片显示
- 修复多个组件直接使用 referenceImage 导致的加载错误

### 影响范围
- 修改文件: 9 个
- 新增文件: 1 个（dbConfig.ts）
- 代码变更: +246 行新增，-148 行删除

---

## [2026-03-06] - 修复本地图片显示和优化自动保存

### 新增功能
- ✅ 添加内容变化检测机制（hash 计算）
  - 避免内容未变化时触发服务器保存
  - 提升性能和用户体验
  - 减少不必要的网络请求

### 修复问题
- ✅ 修复九宫格分镜图片显示问题
  - ShotWorkbench.tsx: 添加 `getImageUrl` 处理本地图片
  - NineGridPreview.tsx: 添加 `getImageUrl` 处理本地图片
  - StageDirector/index.tsx: 修复九宫格图片裁剪和整图使用
  
- ✅ 修复首帧图片显示问题
  - KeyframeEditor.tsx: 添加 `getImageUrl` 处理本地图片
  - ShotCard.tsx: 添加 `getImageUrl` 处理本地图片

- ✅ 修复智谱模型 API Key 检测
  - apiCore.ts: 改进 API Key 检测逻辑
  - shotService.ts: 九宫格分镜使用激活模型

### 改进优化
- ✅ 改进错误日志
  - videoService.ts: 添加详细的 API 响应日志
  - 添加完整的错误信息输出
  - 便于调试和问题排查

### 文档更新
- ✅ 创建项目状态文档
  - 00-项目总览.md: 项目整体状态
  - 01-功能模块.md: 各功能模块详细状态
  - 02-技术架构.md: 技术架构说明
  - 03-开发指南.md: 开发者指南

### 技术细节
- 添加了 `computeProjectHash` 函数用于内容变化检测
- 使用简单的字符串哈希算法（类似 Java 的 String.hashCode）
- 在 `App.tsx` 中实现自动保存优化
- 在多个组件中添加 `getImageUrl` 函数调用

### 影响范围
- 修改文件: 15 个
- 新增文件: 5 个（文档）
- 代码变更: +2575 行新增，-145 行删除

---

## [2026-03-05] - 九宫格分镜功能

### 新增功能
- ✅ 实现九宫格分镜功能
  - AI 拆分视角为 3x3 网格
  - 支持自定义视角描述
  - 支持选择单个视角作为首帧
  - 支持整图作为首帧

### 改进优化
- ✅ 添加本地图片存储
  - IndexedDB 存储图片
  - 自动清理过期数据
  - 支持 blob URL 转换

- ✅ 优化模型配置界面
  - 改进模型列表显示
  - 优化模型切换体验
  - 添加模型激活状态显示

### 技术细节
- 实现 `generateNineGridPanels` 函数
- 实现 `generateNineGridImage` 函数
- 添加 `cropPanelFromNineGrid` 函数
- 创建 NineGridPreview 组件

---

## [2026-03-04] - 本地存储优化

### 新增功能
- ✅ 实现本地图片存储
  - IndexedDB 存储图片
  - 支持大文件存储
  - 自动清理过期数据

### 改进优化
- ✅ 优化图片加载性能
  - 使用 blob URL 减少网络请求
  - 添加图片缓存机制
  - 支持懒加载

### 技术细节
- 创建 `imageStorageService`
- 实现 `saveImage` 函数
- 实现 `getImage` 函数
- 实现 `cleanupOldImages` 函数

---

## [2026-03-03] - 模型配置优化

### 新增功能
- ✅ 添加模型配置界面
  - 支持多模型管理
  - 支持模型激活状态
  - 支持自定义模型配置

### 改进优化
- ✅ 优化模型注册表
  - 支持动态模型注册
  - 支持模型参数配置
  - 支持模型分类

### 技术细节
- 创建 `modelRegistry` 服务
- 实现 `registerModel` 函数
- 实现 `getActiveModel` 函数
- 实现 `getModelById` 函数

---

## [2026-03-02] - 用户认证系统

### 新增功能
- ✅ 实现用户认证系统
  - 登录功能
  - 注册功能
  - 自动登录
  - Token 管理

### 改进优化
- ✅ 集成 Supabase Auth
  - 支持多种登录方式
  - 支持密码重置
  - 支持邮箱验证

### 技术细节
- 创建 `authStore` 状态管理
- 实现 `login` 函数
- 实现 `register` 函数
- 实现 `logout` 函数

---

## [2026-03-01] - 项目初始化

### 新增功能
- ✅ 实现项目创建功能
  - 支持新项目创建
  - 支持项目列表管理
  - 支持项目删除

### 改进优化
- ✅ 优化项目加载性能
  - 添加加载动画
  - 优化数据加载逻辑
  - 支持错误处理

### 技术细节
- 创建 `hybridStorageService`
- 实现 `saveProject` 函数
- 实现 `loadProject` 函数
- 实现 `deleteProject` 函数

---

## 计划中功能

### 短期计划 (1-2 周)
- [ ] 修复 CogVideoX 生成失败问题
- [ ] 优化视频生成速度
- [ ] 改进错误提示信息
- [ ] 添加视频配音功能

### 中期计划 (1-2 月)
- [ ] 支持视频剪辑功能
- [ ] 支持多镜头批量生成
- [ ] 优化移动端体验
- [ ] 添加快捷键支持

### 长期计划 (3-6 月)
- [ ] 支持自定义模型
- [ ] 添加模型使用统计
- [ ] 实现协作功能
- [ ] 优化导出功能

---

## 已知问题

### 当前问题
- 🔄 CogVideoX Flash 偶尔生成失败
  - 状态：正在调查
  - 已添加详细日志
  - 等待更多错误信息

### 已解决问题
- ✅ 本地图片显示问题（2026-03-06）
- ✅ 自动保存性能问题（2026-03-06）
- ✅ 智谱模型 API Key 检测问题（2026-03-06）

---

## 贡献者

感谢所有为项目做出贡献的开发者！

---

## 许可证

MIT License