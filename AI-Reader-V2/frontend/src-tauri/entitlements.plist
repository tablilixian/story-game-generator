<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT for PyInstaller sidecar -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (PyInstaller) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation — required for PyInstaller onefile mode
         on macOS 15+ where extracted dylibs have mismatched Team IDs -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client (HTTP/WS to localhost sidecar) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Network server (sidecar listens on localhost) -->
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
