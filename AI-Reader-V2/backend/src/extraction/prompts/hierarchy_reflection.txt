你是一位地理/空间关系专家。以下是从小说《{title}》中提取的地点父子关系。
请判断每对关系是否正确。

判断标准：
- "父" 必须在物理/行政上**包含** "子"（子在父的内部/辖区内）
- 如果两者是**同级并列**关系（如两座府邸、两座城池），应标记为 "sibling"
- 如果方向反了（子实际包含父），应标记为 "reverse"
- 如果关系正确，标记为 "correct"
- 如果你**无法从小说内容中找到足够依据**来判定，标记为 "uncertain"

⚠️ 关键要求：每条判定必须在 evidence 字段中引用小说原文片段或章节编号作为依据。
没有原文依据的判定应标记为 "uncertain"，不要猜测。

待验证的关系：
{pairs_text}

请以 JSON 数组返回，每项包含：
{{"child": "...", "parent": "...", "verdict": "correct|sibling|reverse|uncertain", "reason": "简要理由", "evidence": "原文依据或章节编号"}}
